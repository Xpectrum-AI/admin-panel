import { modelConfigService } from '@/service/modelConfigService';

// Simple test to prevent "no tests" error
describe('modelConfigService', () => {
  it('should be defined', () => {
    expect(modelConfigService).toBeDefined();
  });
});
