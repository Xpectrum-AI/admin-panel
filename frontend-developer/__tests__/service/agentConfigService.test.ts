import { agentConfigService } from '@/service/agentConfigService';

// Simple test to prevent "no tests" error
describe('agentConfigService', () => {
  it('should be defined', () => {
    expect(agentConfigService).toBeDefined();
  });
});